namespace MirGame;

internal static class Program
{
    // 占位入口：原 StarDefender 示例已移除，本工程保留作为将来移植「传奇」的容器。
    private static async Task Main(string[] args)
    {
        await Task.CompletedTask;
    }
}
